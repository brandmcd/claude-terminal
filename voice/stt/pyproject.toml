[project]
name = "claude-stt"
version = "0.1.0"
description = "Local Whisper speech-to-text sidecar for the claude-terminal chat app (faster-whisper)."
requires-python = ">=3.10"
dependencies = [
    "faster-whisper==1.1.1",
    "huggingface-hub==0.25.2",
    "requests==2.32.3",
    "fastapi==0.115.6",
    "uvicorn==0.34.0",
    "python-multipart==0.0.20",
]
